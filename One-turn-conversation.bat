@echo off
REM This script assumes that Python is in your system's PATH and all
REM required packages (from requirements.txt) have already been installed
REM in your Python environment (e.g., using 'pip install -r requirements.txt').
echo Starting Conversation Archiver...
python app.py
